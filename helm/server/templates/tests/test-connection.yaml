apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wbs.fullname" . }}-test-connection"
  labels:
    {{- include "wbs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wbs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
