apiVersion: v1
kind: Service
metadata:
  name: {{ include "wbs.fullname" . }}
  labels:
    {{- include "wbs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wbs.selectorLabels" . | nindent 4 }}
